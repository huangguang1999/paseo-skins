# Changelog

## Unreleased

- **S（背景）**：公开主题卡复制的 `apply <theme-id>` 在首次使用时只启动前台 Watcher；用户关闭终端、退出 Paseo 或重启电脑后，主题不会自动恢复，与一键换肤的使用预期不一致。
- **T（任务）**：让网站、主题详情和 Agent Skill 的公开换肤流程显式安装持久 Guardian，同时保留 CLI 的临时前台模式和零强退边界。
- **A（行动）**：为 `apply` 新增可见的 `--persist` 选项；公开命令统一携带该选项并安装或原位更新当前用户的 macOS LaunchAgents。若 Paseo 已运行但没有 CDP，命令只完成持久配置并要求用户正常重启；省略该选项仍使用前台 Watcher。同步更新 Skill、帮助、架构约束和六态所有权测试。
- **R（结果）**：同事从网站复制命令后可以关闭终端，主题仍会在 Paseo 重启和 macOS 重新登录后自动恢复；`autostart uninstall` 继续提供完整回退。

- **S（背景）**：主题卡元数据曾使用 7px，卡片操作使用 8px，作者、下载按钮和 Studio 调色板使用 7–9px；完整预览、文档与 CLI 说明也各自维护零散小字号，桌面端留白充足但信息仍难读。
- **T（任务）**：在保留现有编辑感和三列密度的前提下，提高整个公开站点的文字层级、按钮可读性与跨页面一致性。
- **A（行动）**：建立 10/11/12/13/14px 五级字体变量；卡片标题提升至 19px，正文统一到 14px，卡片元数据、来源、操作、Studio、文档和代码分别映射到共享层级，同时扩大下载与卡片操作的命中高度，并收敛首页、内页与 Studio 的移动端大标题以避免中文断词。主题详情会折叠当前目录中重复的中英文名。微缩 Paseo 预览保持原比例并被明确豁免。
- **R（结果）**：公开页面不再出现 10px 以下内容文字，主题卡、完整预览、Studio、文档和移动端共享同一可读性基线；390px 下载页的长命令不再撑宽页面，并由自动化测试阻止微小字号与关键移动端布局回归。

- **S（背景）**：网站复制的 `apply <theme-id>` 在 autostart Guardian 已守护旧主题时会被单 Watcher 锁直接拒绝；同时 Node.js 25 在高 DPI Paseo 窗口捕获大体积 PNG 时会关闭 WebSocket，JPEG 的旧 `fromSurface: false` 参数也可能返回无法截图。
- **T（任务）**：让公开的一键命令在现有常驻主题上也能安全切换，并保证 status、verify、截图、reload 和回退形成可执行闭环。
- **A（行动）**：为 autostart 持久化受限的运行配置并兼容读取旧 Guardian；`apply` 根据所有权状态选择前台启动、幂等成功、Guardian 重配置或手动冲突拒绝，且只在 Watcher 与 renderer 主题 ID 同时匹配后返回成功。截图统一使用 surface 捕获，高 DPI PNG 按 CSS 像素缩放以限制 WebSocket 消息体积。
- **R（结果）**：真实环境已完成 `blue-hair-sofa → cecilylove002` 切换、同主题重复执行、reload 持久化及 Node.js 25 JPEG/PNG 截图验证，过程中始终只有一个 Watcher。

- **S（背景）**：画廊缩略图和完整模拟器使用了自绘的旧式小窗口，包含不存在的 Mac 标题栏与任务卡，侧栏、工具栏、主画布和输入框比例均与当前 Paseo 不符。
- **T（任务）**：以真实 Paseo“新建 workspace”页面为视觉真值，统一修正所有主题预览，而不是逐卡片覆盖样式。
- **A（行动）**：新增共享的 `paseo-preview-frame`，按 23.2% 侧栏、4.5% 主工具栏、全画布背景、workspace 上下文与大输入框重建结构；侧栏继续按真实 Workspace 树还原方形根标识、状态圆点、仅圆环选中态、代码增删计数与轮廓工具图标。画廊、完整模拟器和 Studio 共用同一 DOM，并加入结构回归测试与真实浏览器截图门禁。
- **R（结果）**：30 套主题在缩略图和完整预览中使用同一套当前 Paseo 布局，侧栏不再退化为简化分组卡片；浅色、深色和人物背景只改变主题图与声明式颜色，不再改变产品结构。

- **S（背景）**：公开主题库只有 12 套，且此前扩容方案误把 DreamSkin 热门主题当成视觉参考重新绘制，没有满足“直接适配真实主题包、原图不改”的产品目标。
- **T（任务）**：把 DreamSkin 热门榜前 30 个公开主题包转换为可直接用于 Paseo 的 Theme v2 下载包，同时保持原图、作者、许可证与来源可追溯，并隔离上游可执行样式。
- **A（行动）**：新增确定性 DreamSkin 适配器和受限 ZIP 读取器，逐包校验官方包体 SHA-256、内部文件哈希与图片元数据；原背景图按字节保留，只把声明式颜色、焦点和显示参数映射到 Paseo Theme v2，不复制 `theme.css` 或脚本；图库对齐热门榜的三列卡片、排序、分页和直接下载流程，并在目录、素材清单与包内说明中记录原包和原图哈希。
- **R（结果）**：网站可免登录浏览并下载 30 套真实热门主题的 Paseo 适配包；每个 ZIP 仅包含 Paseo 清单、原背景图和来源说明，适配器、发布检查和回归测试共同保证图片未被修改、来源字段一致且上游代码不会进入产物。

- **S（背景）**：浅色主题在不同 Renderer 生命周期下出现了 hover 背景固化、workspace 操作区白色遮罩、深色按钮搭配深色文字，以及全局导航 hover 反馈过弱等问题；单看命中元素或单个页面无法稳定发现这些缺陷。
- **T（任务）**：把本轮问题从一次性修复升级为项目级防复发能力，同时保持声明式主题、可逆 CDP 注入和不提交个人视觉证据的安全边界。
- **A（行动）**：统一交互控件的有效背景对比度计算和侧栏状态 CSS，清理 kebab 与 SVG scrim；新增覆盖 21 类页面、5 类 hover、嵌套文字节点、内联背景、辅助层及状态恢复的 `audit:renderer` 命令；补充 Renderer 样式安全手册、架构不变量、贡献规范、Agent 规则、PR 检查项和回归测试。
- **R（结果）**：深色填充控件达到至少 4.5 的文字对比度，侧栏 hover 可见且移出后无残留；真实 Paseo 巡检可输出结构化报告，并在结束后恢复原路径与侧栏滚动位置。

## 0.9.0 - 2026-08-09

- **S（背景）**：原官网是单页暗色画廊，缺少完整模拟器、独立主题库、可撤销 Studio 与按主题 ID 应用的快捷命令；同时同 ID 主题更新不会刷新注入缓存，侧栏 hover 会被持久内联背景覆盖。
- **T（任务）**：对齐成熟主题市场的公开功能，在不实现桌面客户端、不扩大 Paseo 注入权限的前提下，完成浏览、预览、制作、应用和还原闭环，并修复现有运行时缺陷。
- **A（行动）**：重构为首页、主题库、在线模拟器、Studio、文档、CLI 与主题详情多页面站点；加入搜索、筛选、排序、本地收藏夹、可分享模拟器状态、首页/任务页、宽窄窗口、侧栏与原版对比；Studio 增加图片拖放、自动取色、SHA-256、撤销/重做/重置、Safe CSS 验证、实时预览和导出；CLI 新增 `apply <theme-id>`，并拆分纯参数解析与分层帮助；Theme v2 扩展稳定的 `dark` / `light` 契约；注入层增加配置签名缓存失效、可销毁的 SPA 路由同步，将侧栏 hover/selected 视觉交给可逆 CSS 状态管理，并阻止全局交互条目的瞬态中性色被固化为内联样式；按社区热门风格补充水墨、月夜、猫咪暖室、奶油极简、海盐玻璃和霓虹终端 6 套原创主题。
- **R（结果）**：静态官网覆盖从发现到制作的核心交互，公开主题从 6 套增加到 12 套，程序员可以用一条公开命令安全换肤；桌面与 390 px 移动端均完成真实截图验收，74 项自动化测试和内部链接检查通过；Paseo 0.3.0 的 workspace、计划、历史和新建页完成真实路由检查，菜单、全局交互条目和非选中 workspace 行的 hover 进出均无残留内联背景。

## 0.8.0 - 2026-08-08

- **S（背景）**：皮肤是运行时通过 CDP 注入渲染进程内存的样式，不落盘；Paseo 一旦经 Dock、Spotlight、自动更新或开机重启，新进程既不带调试端口也丢失注入，用户必须手动重跑 `npm start` 才能恢复皮肤。
- **T（任务）**：让皮肤在任何方式重启 Paseo 后自动恢复，无需人工干预，同时保持不 patch、不重启 Paseo 的安全边界。
- **A（行动）**：新增 `autostart` 命令与 `src/autostart.mjs`，安装两个仅作用于当前用户的 macOS 登录代理——`com.paseo-skins.cdp-env` 通过 `launchctl setenv` 注入 `PASEO_ELECTRON_FLAGS` 使任何启动方式都开启回环 CDP，`com.paseo-skins.guardian` keepalive 守护 `inject` watcher 并跟随重启与新窗口；路径全部动态解析（node 可执行文件、CLI、home 目录），plist 对路径做 XML 转义，非 macOS 明确报错；补充 `autostart:install/uninstall/status` 脚本、单元测试、README 与 check 覆盖。
- **R（结果）**：用户执行一次 `npm run autostart:install` 后，皮肤在每次 Paseo 重启后自动注入回来，`autostart:status` 可查代理状态，`autostart:uninstall` 干净移除；自启机制不改动 Paseo 应用、不重启 daemon。

## 0.7.0 - 2026-07-22

- **S（背景）**：同类 Codex 主题项目已经形成一图生成、可视化编辑、声明式主题包、供应链证明和社区投稿等成熟能力；本项目虽能安全注入 Paseo，但主题制作、并发保护、素材审计和发布可信度仍有明显差距。
- **T（任务）**：把 Paseo Skins 从单一皮肤加载器升级为可发现、可制作、可验证、可分发且适合 Agent 自动操作的完整主题平台。
- **A（行动）**：新增 Theme v2 JSON Schema 与图片 SHA-256 完整性校验；实现浏览器和 CLI 一图自动取色生成主题、目录读取与只读检查；加入 watcher 进程锁、素材 provenance 审计、CodeQL、Dependabot、跨平台 CI、Release 校验和与 artifact attestation；补齐 GitHub SEO、基准研究、兼容矩阵、投稿模板和社区文档。
- **R（结果）**：六套公开主题全部升级为可验证的 Theme v2，用户可从任意有权使用的图片本地生成新主题，Agent 拥有完整的发现—检查—应用—验收—还原路径，发布物和素材来源也能被机器审计。

## 0.6.0 - 2026-07-22

- **S（背景）**：仓库已有公开页面和主题目录，但搜索引擎只能稳定读取单一首页，GitHub 与社交平台也缺少完整的项目语义和分享预览。
- **T（任务）**：让用户能通过 Paseo theme、Paseo skin、Agent Skill、CDP loader 等准确关键词发现项目，并让每套主题拥有可独立收录的入口。
- **A（行动）**：补充双语 README、包元数据、canonical、Open Graph、Twitter Card、SoftwareApplication JSON-LD、robots 与 sitemap；构建六套静态主题详情页、内部索引和 1280×640 站点社交预览图；扩展 GitHub About 与 topics。
- **R（结果）**：GitHub 站内、Google/Bing 抓取和社交分享现在都能读取明确项目描述，六套主题可通过独立 URL 被发现，同时保留原有动态画廊与 Agent 一键接入体验。

## 0.5.0 - 2026-07-22

- **S（背景）**：网页只能复制终端命令，用户仍要自己判断环境、处理 Paseo 运行状态并完成验收，Agent 无法直接接管换肤流程。
- **T（任务）**：让用户能把任意主题一键交给自己的 Agent，并支持长期安装成标准 Agent Skill。
- **A（行动）**：新增 `paseo-skins` 标准 Skill、Agent UI 元数据与安全工作流；主题卡主操作改为复制完整 Agent 任务；增加全局 Skill 接入入口、终端备用入口，并将 Skill 同步发布到网站根路径。
- **R（结果）**：未安装 Skill 的 Agent 可通过公开 URL 临时执行，Codex、Claude Code、Cursor 等兼容 Agent 可用一条 `npx skills add` 命令长期接入。

## 0.4.1 - 2026-07-22

- **S（背景）**：首版画廊把同一张武侠壁纸包装成三个配色预设，视觉上仍是一个主题，偏离皮肤市场应提供多种背景图片的核心价值。
- **T（任务）**：让每张主题卡真正代表一套独立背景和完整视觉方案。
- **A（行动）**：保留原创武侠主题，新增极光雪岭、深空星云、东京雨夜、暖色书房、荒漠落日五张独立图片及对应清单；补齐图片来源、许可证和页面署名，并加入目录唯一性测试。
- **R（结果）**：公开画廊现有六套视觉明显不同、可独立安装的主题，CI 会阻止重复背景被伪装成不同主题。

## 0.4.0 - 2026-07-22

- **S（背景）**：原项目只能加载本地主题，其他 Paseo 用户缺少统一的发现、预览与安装入口。
- **T（任务）**：建设可公开分发的通用皮肤画廊，并让用户从主题卡片一键复制可执行的安装命令。
- **A（行动）**：新增响应式静态画廊、搜索筛选、主题详情与复制反馈；建立远程主题下载、同源目录约束、体积限制和缓存；补充 3 套原创配色预设、GitHub Pages 工作流与远程加载测试。
- **R（结果）**：用户可从网页选择主题并用一行命令换肤，CLI 全程只处理声明式 JSON 与图片，不执行远程主题代码。

## 0.3.1 - 2026-07-22

- **S（背景）**：夜巡主题的 SVG 聚光灯和星点压过原壁纸，侧边栏与底部栏又使用高不透明实色面板，产生明显割裂。
- **T（任务）**：保留壁纸自身光影，让导航与输入区域自然融入同一幅夜林画面。
- **A（行动）**：删除整层 SVG 舞台光效，降低边缘遮罩强度；将侧栏和底栏改为同色半透明磨砂材质，并把四边描边收敛为单侧低对比边线。
- **R（结果）**：画面不再出现人工聚光灯，背景能透过两侧 chrome 连续呈现，同时维持文字与输入区域可读性。

## 0.3.0 - 2026-07-21

- **S（背景）**：用户选定 Wallhaven 2 号夜林军阵壁纸，希望视觉配色与图片统一，同时保留原黑金主题。
- **T（任务）**：将新壁纸设为默认主题，并让首页、侧栏、选中态和光效形成一致的冷蓝夜巡风格。
- **A（行动）**：新增「不良人·夜巡」主题，扩展可向后兼容的 `panelAlt` / `glow` 色板，按主题颜色映射磨砂面、边框、选中态、滚动条和背景光效，补充来源与权利说明，并让 4K 验收截图支持更稳定的 JPEG 输出。
- **R（结果）**：私有原型完成深海军蓝、冷钢蓝与暗红点缀方案；因素材再分发权不明确，该主题不进入公开仓库或网站。

## 0.2.0 - 2026-07-21

- **S（背景）**：初版已经能通过 CDP 注入主题，但缺少主题清单、只读诊断、验收命令和 reload hook 注销，恢复边界不够完整。
- **T（任务）**：对齐成熟 Codex 皮肤项目的安全生命周期与开源仓库基础标准，同时保持 Paseo 零 patch、零 daemon 重启。
- **A（行动）**：新增 `theme.json` 校验、路由降噪、`doctor`、`verify`、截图、WebSocket 回环校验、watcher 清理、CI、安全文档与贡献指南。
- **R（结果）**：主题可以独立校验和替换，停止 watcher 后不再残留 reload hook，并可用结构化结果证明 renderer 根节点与主题状态正常。

## 0.1.0 - 2026-07-20

- **S（背景）**：Paseo 暂无官方自定义皮肤接口。
- **T（任务）**：在不修改 Paseo 安装包的前提下实现舞台黑金主题。
- **A（行动）**：实现 loopback CDP 启动、renderer watcher、原创 Hero 图、DOM 颜色适配和 reset。
- **R（结果）**：完成「舞台黑金·暗夜江湖」首个可用版本。
